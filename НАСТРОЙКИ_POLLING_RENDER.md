# ⚙️ Настройки Polling для работы на Render

## 📋 Обзор

Бот использует **long polling** для получения обновлений от Telegram Bot API. Для стабильной работы на Render и других облачных платформах настроены оптимальные параметры таймаутов и обработки ошибок.

---

## 🔧 Текущие настройки

### Polling параметры

```python
polling_timeout = 60  # секунд
polling_limit = 100   # обновлений за запрос
session_timeout = 30  # секунд (HTTP клиент)
```

### Что означают эти параметры:

1. **`polling_timeout = 60`** (60 секунд)
   - Время ожидания обновлений от Telegram сервера
   - Telegram будет держать соединение открытым до 60 секунд, ожидая новые обновления
   - Большее значение = меньше запросов, но больше задержка при получении обновлений
   - **60 секунд** - оптимальный баланс для Render

2. **`polling_limit = 100`** (100 обновлений)
   - Максимальное количество обновлений, получаемых за один запрос
   - Telegram позволяет от 1 до 100 обновлений
   - **100** - максимум для эффективности (меньше HTTP запросов)

3. **`session_timeout = 30`** (30 секунд)
   - Таймаут для HTTP клиента (aiohttp сессии)
   - Используется для HTTP запросов к Telegram API
   - Автоматически увеличивается до `session_timeout + polling_timeout` для long polling запросов

---

## 🛡️ Механизмы защиты от зависаний

### 1. Автоматический перезапуск при отсутствии обновлений

Если бот не получает обновления более **5 минут**, автоматически перезапускается polling:

```python
no_updates_timeout = 300  # 5 минут
```

**Логи:**
```
⚠️ Нет обновлений 300 секунд (5.0 минут). Возможно polling завис.
✅ Соединение работает, но обновления не приходят. Перезапуск polling...
```

### 2. Профилактический перезапуск

Каждые **6 часов** polling автоматически перезапускается для предотвращения проблем после длительного простоя:

```python
preventive_restart_interval = 21600  # 6 часов
```

**Логи:**
```
⏰ Polling работает уже 6.0 часов. Профилактический перезапуск...
```

### 3. Health Check мониторинг

Проверка соединения каждые **5 минут**:

```python
connection_check_interval = 300  # 5 минут
```

**Что проверяется:**
- Работает ли соединение с Telegram API
- Получает ли бот обновления
- Нет ли зависаний в polling

---

## 📊 Логирование

### При запуске бота вы увидите:

```
📋 Настройки polling:
   • polling_timeout: 60 сек
   • session_timeout: 30 сек
   • limit: 100 обновлений за запрос
   • allowed_updates: ['message', 'callback_query', ...]

⚙️ Настройки для работы на Render:
   • Long polling с таймаутом 60 секунд
   • Автоматический перезапуск при отсутствии обновлений > 5 минут
   • Профилактический перезапуск каждые 6 часов
   • Health check endpoint: /health
```

### При работе бота:

```
✅ Health check: последнее обновление: 45 сек назад
🔍 Проверка соединения с Telegram API...
✅ Health check: бот работает, username: @your_bot, последнее обновление: 12 сек назад
```

---

## 🔍 Диагностика проблем

### Проблема: Бот перестал отвечать на Render

**Причины:**
1. Polling завис (нет обновлений > 5 минут)
2. Соединение с Telegram API разорвано
3. Render инстанс "заснул" (нет активности)

**Решение:**
- Бот автоматически перезапустит polling при обнаружении проблемы
- Health check endpoint `/health` предотвращает "засыпание" инстанса
- Проверьте логи на Render для диагностики

### Проблема: Долгая задержка ответа

**Причины:**
1. `polling_timeout` слишком большой
2. Много обновлений в очереди

**Решение:**
- Уменьшите `polling_timeout` до 20-30 секунд (но не меньше 10)
- Увеличьте `polling_limit` до 100 для обработки большего количества обновлений

### Проблема: Частые перезапуски

**Причины:**
1. Нестабильное соединение с Telegram API
2. Render инстанс перезапускается

**Решение:**
- Увеличьте `polling_timeout` до 60-90 секунд
- Увеличьте `no_updates_timeout` до 600 секунд (10 минут)
- Проверьте стабильность интернет-соединения на Render

---

## ⚙️ Изменение настроек

### Где находятся настройки:

**Файл:** `bot.py`

**Строка ~79:** Инициализация Bot
```python
bot = Bot(
    token=BOT_TOKEN,
    session_timeout=30  # Измените здесь
)
```

**Строка ~1950:** Настройки polling
```python
polling_timeout = 60  # Измените здесь
polling_limit = 100   # Измените здесь
```

**Строка ~1844:** Таймауты для health check
```python
no_updates_timeout = 300  # 5 минут - измените здесь
connection_check_interval = 300  # 5 минут - измените здесь
preventive_restart_interval = 21600  # 6 часов - измените здесь
```

### Рекомендуемые значения для Render:

| Параметр | Минимум | Рекомендуется | Максимум | Описание |
|----------|---------|---------------|----------|----------|
| `polling_timeout` | 10 | **60** | 120 | Таймаут long polling |
| `polling_limit` | 1 | **100** | 100 | Обновлений за запрос |
| `session_timeout` | 10 | **30** | 60 | HTTP клиент таймаут |
| `no_updates_timeout` | 180 | **300** | 600 | Таймаут без обновлений |
| `preventive_restart_interval` | 3600 | **21600** | 86400 | Профилактический перезапуск |

---

## 📝 Health Check для Render

Бот автоматически предоставляет health check endpoint:

- **URL:** `https://your-app.onrender.com/health`
- **Метод:** GET
- **Ответ:** `{"status": "ok", "service": "tower-bot-telegram"}`

Этот endpoint предотвращает "засыпание" инстанса на Render.

**Настройка в render.yaml:**
```yaml
healthCheckPath: /health
```

---

## 🔄 Как работает перезапуск

1. **Health check обнаруживает проблему:**
   - Нет обновлений > 5 минут
   - Ошибка соединения с Telegram API
   - Прошло 6 часов работы

2. **Polling останавливается:**
   - Устанавливается флаг `polling_active['status'] = False`
   - Отменяется polling task
   - Закрывается текущая сессия

3. **Новый polling запускается:**
   - Создается новая сессия
   - Удаляются pending updates
   - Запускается новый polling с теми же настройками

4. **Логирование:**
   - Все действия логируются для диагностики
   - Можно отследить причину перезапуска

---

## ✅ Проверка работы

### 1. Проверьте логи на Render:

Должны быть сообщения:
```
✅ Бот подключен: @your_bot (ID: ...)
🔄 Начинаю polling...
📋 Настройки polling: ...
```

### 2. Проверьте health check:

```bash
curl https://your-app.onrender.com/health
```

Должен вернуть:
```json
{"status": "ok", "service": "tower-bot-telegram"}
```

### 3. Отправьте сообщение боту:

Бот должен ответить в течение нескольких секунд.

---

## 🆘 Если проблемы остаются

1. **Проверьте логи на Render:**
   - Зайдите в Dashboard → Logs
   - Найдите ошибки или предупреждения

2. **Проверьте переменные окружения:**
   - `BOT_TOKEN` - правильный токен бота
   - `ADMIN_IDS` - ID администраторов
   - `PORT` - порт для HTTP сервера (автоматически на Render)

3. **Попробуйте изменить настройки:**
   - Увеличьте таймауты если частые перезапуски
   - Уменьшите таймауты если долгая задержка ответа

4. **Обратитесь за помощью:**
   - Менеджер: @nomep999
   - Проверьте документацию aiogram: https://docs.aiogram.dev/

---

## 📚 Дополнительная информация

- **Aiogram документация:** https://docs.aiogram.dev/en/latest/
- **Telegram Bot API:** https://core.telegram.org/bots/api#getupdates
- **Render документация:** https://render.com/docs

---

## 🔧 Технические детали

### Как работает long polling:

1. Бот отправляет запрос `getUpdates` к Telegram API
2. Telegram держит соединение открытым до `polling_timeout` секунд
3. Если есть обновления - возвращает их сразу
4. Если нет обновлений - ждет до таймаута, затем возвращает пустой ответ
5. Бот сразу отправляет новый запрос

### Преимущества long polling:

- ✅ Меньше HTTP запросов (экономия ресурсов)
- ✅ Быстрее получение обновлений (меньше задержка)
- ✅ Меньше нагрузка на Telegram API
- ✅ Стабильнее работа на облачных платформах

### Недостатки:

- ⚠️ Требует стабильное соединение
- ⚠️ Может зависнуть при проблемах с сетью
- ⚠️ Нужны механизмы перезапуска (уже реализованы)

---

**Последнее обновление:** 2024
**Версия aiogram:** 3.13.0+
