# Tower Bot AI - Telegram Bot

Telegram бот для игры Tower Rush с системой депозитов и сигналов.

## Возможности

- 🔐 Система регистрации пользователей
- 💰 Проверка депозита перед доступом к сигналам
- 📊 Генерация сигналов с вероятностью успеха
- 📞 Интеграция с менеджером для обработки депозитов
- 🗄️ База данных SQLite для хранения пользователей

## Установка

1. **Установите Python 3.8+**

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Получите токен бота:**
   - Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Скопируйте полученный токен

4. **Настройте токен и реферальную ссылку:**
   
   **Вариант 1: Через переменные окружения (рекомендуется)**
   ```bash
   # Windows PowerShell
   $env:BOT_TOKEN="ваш_токен_здесь"
   $env:ADMIN_IDS="ваш_telegram_id"
   $env:CASINO_REF_LINK="https://casino.com/?ref={user_id}"
   
   # Windows CMD
   set BOT_TOKEN=ваш_токен_здесь
   set ADMIN_IDS=ваш_telegram_id
   set CASINO_REF_LINK=https://casino.com/?ref={user_id}
   
   # Linux/Mac
   export BOT_TOKEN="ваш_токен_здесь"
   export ADMIN_IDS="ваш_telegram_id"
   export CASINO_REF_LINK="https://casino.com/?ref={user_id}"
   ```
   
   **Вариант 2: Прямо в коде (не рекомендуется для продакшена)**
   - Откройте `bot.py`
   - Найдите строки:
     ```python
     BOT_TOKEN = os.getenv('BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
     ADMIN_IDS = [int(x) for x in os.getenv('ADMIN_IDS', '').split(',') if x.strip()]
     CASINO_REF_LINK_TEMPLATE = os.getenv('CASINO_REF_LINK', 'https://casino.example.com/?ref={user_id}')
     ```
   - Замените значения на свои

5. **Запустите бота:**
   ```bash
   python bot.py
   ```

## Использование

### Для пользователей:

1. Откройте бота в Telegram
2. Отправьте команду `/start`
3. Если депозит не подтвержден:
   - Нажмите "🎰 Получить реферальную ссылку"
   - Перейдите по ссылке и зарегистрируйтесь в казино
   - Сделайте депозит в казино
   - Нажмите "✅ Я сделал депозит" и отправьте подтверждение менеджеру
4. После подтверждения депозита администратором:
   - Вы получите уведомление о подтверждении
   - Нажмите "📊 Получить сигнал"
   - Получайте сигналы с вероятностью успеха

### Для администратора:

**Обновление реферальной ссылки:**
```
/set_referral <новая_ссылка>
```

Пример:
```
/set_referral https://t.me/LB_Chainreak_bot/app?startapp=bXN0PTB4NDkxYjMxMTcmbT1zdG9wa2EmYz1Ic2pzag
```

**Подтверждение депозита пользователя:**

Вариант 1: Через команду с ID:
```
/approve_deposit <user_id> [amount]
```

Вариант 2: Через ответ на сообщение (быстрее):
```
Ответьте на сообщение пользователя и напишите: /approve [amount]
```

Вариант 3: Через кнопку в уведомлении:
- Когда пользователь отправляет подтверждение депозита, администратор получает уведомление с кнопками
- Нажмите "✅ Подтвердить депозит" для автоматического подтверждения

Примеры:
```
/approve_deposit 123456789 1000
/approve 1000  (при ответе на сообщение пользователя)
```

**Просмотр статистики:**
```
/stats
```

Показывает общую статистику по пользователям и депозитам.

## Структура проекта

```
tower-bot-telegram/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── README.md          # Документация
└── tower_bot.db       # База данных (создается автоматически)
```

## База данных

Бот использует SQLite базу данных `tower_bot.db` со следующей структурой:

**Таблица `users`:**
- `user_id` - ID пользователя Telegram (PRIMARY KEY)
- `username` - Имя пользователя Telegram
- `first_name` - Имя пользователя
- `registered_at` - Дата регистрации
- `has_deposit` - Флаг наличия депозита (0/1)
- `deposit_amount` - Сумма депозита
- `deposit_proof` - Подтверждение депозита (текст/ссылка)
- `deposit_date` - Дата подтверждения депозита
- `referral_link_used` - Флаг использования реферальной ссылки (0/1)
- `casino_account` - Логин/ID в казино
- `deposit_verified` - Флаг подтверждения депозита администратором (0/1)

## Безопасность

⚠️ **Важно:**
- Никогда не публикуйте токен бота в открытом доступе
- Используйте переменные окружения для хранения токена
- Добавьте проверку прав администратора для команды `/approve_deposit`
- Регулярно делайте резервные копии базы данных

## Система реферальных ссылок

Бот использует систему реферальных ссылок для отслеживания депозитов:

1. **Формат ссылки:**
   - Каждый пользователь получает уникальную ссылку с его `user_id`
   - Формат: `https://casino.com/?ref={user_id}`
   - Вы можете настроить формат в переменной `CASINO_REF_LINK_TEMPLATE`

2. **Процесс работы:**
   - Пользователь получает реферальную ссылку через бота
   - Регистрируется в казино по этой ссылке
   - Делает депозит
   - Отправляет подтверждение администратору
   - Администратор подтверждает через `/approve_deposit`

3. **Проверка депозита:**
   - В данный момент используется ручная проверка администратором
   - Можно добавить API интеграцию с казино для автоматической проверки

## Доработки для продакшена

1. **Интеграция с API казино:**
   - Автоматическая проверка депозитов через API
   - Отслеживание регистраций по реферальным ссылкам
   - Автоматическое подтверждение депозитов

2. **Добавить логирование действий:**
   - Логирование всех депозитов
   - Логирование использования реферальных ссылок
   - Логирование использования команд
   - Логирование ошибок

3. **Улучшить обработку депозитов:**
   - Автоматическая отправка уведомлений менеджеру
   - Интеграция с платежными системами
   - Автоматическая проверка платежей

4. **Добавить статистику:**
   - Количество пользователей
   - Количество активных пользователей
   - Статистика по депозитам
   - Статистика по реферальным ссылкам

## Поддержка

- Менеджер: [@nomep999](https://t.me/nomep999)
- ТГК: [@maksoncikaz](https://t.me/maksoncikaz)

## Лицензия

Проект создан для личного использования.
