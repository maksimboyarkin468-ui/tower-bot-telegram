# 🚀 Инструкция: Деплой улучшений на Render

## ✅ Что нужно сделать

### Вариант 1: Если Render подключен к Git репозиторию (автоматический деплой)

#### Шаг 1: Закоммитьте изменения

Откройте терминал (PowerShell) в папке бота:

```powershell
cd C:\Users\boiar\tower-bot-telegram
```

Проверьте, какие файлы изменены:

```powershell
git status
```

Должны быть изменения в:
- `bot.py` (настройки polling)
- `НАСТРОЙКИ_POLLING_RENDER.md` (новая документация)
- `УЛУЧШЕНИЯ_POLLING.md` (новая документация)

Добавьте все изменения:

```powershell
git add bot.py НАСТРОЙКИ_POLLING_RENDER.md УЛУЧШЕНИЯ_POLLING.md ИНСТРУКЦИЯ_ДЕПЛОЙ_RENDER.md
```

Или добавьте все файлы:

```powershell
git add .
```

Закоммитьте изменения:

```powershell
git commit -m "Улучшены настройки polling для стабильной работы на Render"
```

#### Шаг 2: Запушьте в репозиторий

```powershell
git push
```

#### Шаг 3: Дождитесь автоматического деплоя

1. Зайдите на сайт Render: https://dashboard.render.com
2. Выберите ваш сервис `tower-bot-telegram`
3. В разделе **"Events"** или **"Logs"** вы увидите процесс деплоя
4. Дождитесь сообщения **"Deploy successful"** или **"Live"**

---

### Вариант 2: Если нужно обновить код вручную на Render

#### Шаг 1: Зайдите в Dashboard Render

1. Откройте https://dashboard.render.com
2. Войдите в свой аккаунт
3. Найдите сервис `tower-bot-telegram`

#### Шаг 2: Обновите код

**Если используется Git репозиторий:**
- Нажмите кнопку **"Manual Deploy"** → **"Deploy latest commit"**
- Или просто запушьте изменения в git (см. Вариант 1)

**Если код загружается вручную:**
- Нажмите **"Settings"** → найдите раздел **"Build & Deploy"**
- Загрузите обновленный файл `bot.py`

#### Шаг 3: Запустите деплой

Нажмите кнопку **"Manual Deploy"** или дождитесь автоматического деплоя.

---

## 🔍 Проверка после деплоя

### Шаг 1: Проверьте логи на Render

1. В Dashboard Render выберите ваш сервис
2. Перейдите в раздел **"Logs"**
3. Найдите сообщения о запуске бота

**Должны быть такие сообщения:**

```
✅ Бот подключен: @your_bot (ID: ...)
🔄 Начинаю polling...
📋 Настройки polling:
   • polling_timeout: 60 сек
   • session_timeout: 30 сек
   • limit: 100 обновлений за запрос
⚙️ Настройки для работы на Render:
   • Long polling с таймаутом 60 секунд
   • Автоматический перезапуск при отсутствии обновлений > 5 минут
   • Профилактический перезапуск каждые 6 часов
   • Health check endpoint: /health
```

**Если видите эти сообщения - всё работает! ✅**

### Шаг 2: Проверьте Health Check

1. В Dashboard Render найдите **"Service URL"** (например: `https://tower-bot-telegram-xxxx.onrender.com`)
2. Откройте в браузере: `https://your-app-url.onrender.com/health`
3. Должен вернуться ответ: `{"status": "ok", "service": "tower-bot-telegram"}`

**Или через командную строку:**

```powershell
curl https://your-app-url.onrender.com/health
```

### Шаг 3: Протестируйте бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. Бот должен ответить в течение нескольких секунд

**Если бот отвечает - всё работает отлично! ✅**

---

## ⚙️ Проверка переменных окружения

Убедитесь, что на Render настроены все переменные окружения:

1. В Dashboard Render выберите ваш сервис
2. Перейдите в **"Environment"** или **"Environment Variables"**
3. Проверьте наличие:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен бота от @BotFather | `123456789:ABCdef...` |
| `ADMIN_IDS` | ID администраторов (через запятую) | `123456789,987654321` |
| `CASINO_REF_LINK` | Реферальная ссылка казино (опционально) | `https://t.me/...` |
| `PORT` | Порт для HTTP сервера | Автоматически на Render |

**Если какой-то переменной нет - добавьте её!**

---

## 🐛 Если что-то не работает

### Проблема: Бот не запускается

**Проверьте логи на Render:**

1. Найдите ошибки в логах (красные сообщения)
2. Частые ошибки:
   - `Unauthorized` → Неправильный токен бота
   - `Module not found` → Не установлены зависимости
   - `Connection timeout` → Проблемы с сетью

**Решение:**
- Проверьте переменную `BOT_TOKEN` в Environment Variables
- Убедитесь, что файл `requirements.txt` содержит все зависимости
- Попробуйте перезапустить сервис: **"Manual Deploy"** → **"Deploy latest commit"**

### Проблема: Бот запустился, но не отвечает

**Проверьте:**

1. **Логи на Render:**
   - Должны быть сообщения `📋 Настройки polling: ...`
   - Должны быть сообщения `✅ Health check: ...`

2. **Health check endpoint:**
   - Откройте `/health` в браузере
   - Должен вернуться `{"status": "ok"}`

3. **Переменные окружения:**
   - Проверьте `BOT_TOKEN` - правильный ли токен
   - Проверьте `ADMIN_IDS` - правильные ли ID

**Решение:**
- Подождите 1-2 минуты после деплоя (боту нужно время на запуск)
- Проверьте логи на наличие ошибок
- Попробуйте отправить `/start` боту еще раз

### Проблема: Частые перезапуски

**Проверьте логи:**

Ищите сообщения:
- `⚠️ Нет обновлений ... секунд`
- `❌ Ошибка при работе бота`
- `ConnectionError`

**Решение:**
- Это нормально, если перезапуски редкие (раз в несколько часов)
- Если перезапуски каждые несколько минут - проверьте токен бота и интернет-соединение
- Увеличьте таймауты в `bot.py` (см. документацию `НАСТРОЙКИ_POLLING_RENDER.md`)

---

## 📋 Чек-лист деплоя

Пройдитесь по списку и убедитесь, что всё сделано:

- [ ] Изменения закоммичены в git (если используется git)
- [ ] Изменения запушены в репозиторий (если используется git)
- [ ] Деплой запущен на Render (автоматически или вручную)
- [ ] Деплой завершился успешно (статус "Live")
- [ ] В логах видны сообщения о настройках polling
- [ ] Health check endpoint `/health` возвращает `{"status": "ok"}`
- [ ] Бот отвечает на команду `/start`
- [ ] Все переменные окружения настроены (`BOT_TOKEN`, `ADMIN_IDS`)

---

## 🎯 Быстрая проверка (30 секунд)

1. **Откройте логи на Render** → Должны быть `📋 Настройки polling: ...`
2. **Откройте `/health`** → Должен вернуться `{"status": "ok"}`
3. **Отправьте `/start` боту** → Должен ответить

**Если всё это работает - деплой успешен! ✅**

---

## 📞 Помощь

Если что-то не работает:

1. **Проверьте логи на Render** - там будет информация об ошибках
2. **Проверьте документацию:**
   - `НАСТРОЙКИ_POLLING_RENDER.md` - подробное описание настроек
   - `УЛУЧШЕНИЯ_POLLING.md` - что было изменено
3. **Обратитесь за помощью:**
   - Менеджер: @nomep999
   - Проверьте документацию Render: https://render.com/docs

---

## 🔄 Что дальше?

После успешного деплоя:

1. **Мониторьте работу бота:**
   - Проверяйте логи раз в день
   - Убедитесь, что бот отвечает на сообщения
   - Проверяйте health check endpoint

2. **Настройте уведомления (опционально):**
   - В Render можно настроить email уведомления о падениях сервиса
   - Settings → Notifications

3. **Оптимизируйте при необходимости:**
   - Если бот часто перезапускается - увеличьте таймауты
   - Если долгая задержка ответа - уменьшите `polling_timeout`
   - См. документацию `НАСТРОЙКИ_POLLING_RENDER.md`

---

**Удачи с деплоем! 🚀**
