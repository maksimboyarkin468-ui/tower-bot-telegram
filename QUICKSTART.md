# Быстрый старт Tower Bot AI

## Шаг 1: Получение токена бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например: `Tower Bot AI`)
   - Введите username бота (например: `tower_bot_ai_bot`)
5. Скопируйте токен (выглядит примерно так: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Узнать свой Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Скопируйте ваш ID (число, например: `123456789`)

## Шаг 3: Установка зависимостей

Откройте терминал в папке `tower-bot-telegram` и выполните:

```bash
pip install -r requirements.txt
```

## Шаг 4: Настройка токена, администратора и реферальной ссылки

### Вариант A: Через переменные окружения (рекомендуется)

**Windows PowerShell:**
```powershell
$env:BOT_TOKEN="ваш_токен_здесь"
$env:ADMIN_IDS="ваш_telegram_id"
$env:CASINO_REF_LINK="https://ваше-казино.com/?ref={user_id}"
```

**Windows CMD:**
```cmd
set BOT_TOKEN=ваш_токен_здесь
set ADMIN_IDS=ваш_telegram_id
set CASINO_REF_LINK=https://ваше-казино.com/?ref={user_id}
```

**Linux/Mac:**
```bash
export BOT_TOKEN="ваш_токен_здесь"
export ADMIN_IDS="ваш_telegram_id"
export CASINO_REF_LINK="https://ваше-казино.com/?ref={user_id}"
```

### Вариант B: Прямо в коде

Откройте `bot.py` и найдите строки:

```python
BOT_TOKEN = os.getenv('BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
ADMIN_IDS = [int(x) for x in os.getenv('ADMIN_IDS', '').split(',') if x.strip()]
CASINO_REF_LINK_TEMPLATE = os.getenv('CASINO_REF_LINK', 'https://casino.example.com/?ref={user_id}')
```

Замените на:

```python
BOT_TOKEN = 'ваш_токен_здесь'
ADMIN_IDS = [ваш_telegram_id]  # Например: [123456789]
CASINO_REF_LINK_TEMPLATE = 'https://ваше-казино.com/?ref={user_id}'
```

**Важно:** В реферальной ссылке используйте `{user_id}` - это будет автоматически заменено на ID пользователя Telegram.

## Шаг 5: Запуск бота

```bash
python bot.py
```

Вы должны увидеть сообщение: `Бот запущен`

## Шаг 6: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Вы должны увидеть приветственное сообщение

## Команды для администратора

**Обновление реферальной ссылки:**
```
/set_referral https://t.me/LB_Chainreak_bot/app?startapp=НОВАЯ_ССЫЛКА
```

**Подтверждение депозита (3 способа):**

1. Через команду:
```
/approve_deposit 123456789 1000
```

2. Через ответ на сообщение (самый быстрый):
```
Ответьте на сообщение пользователя → /approve 1000
```

3. Через кнопку в уведомлении:
- Нажмите "✅ Подтвердить депозит" в уведомлении от бота

**Статистика:**
```
/stats
```

## Структура работы бота

1. **Пользователь без депозита:**
   - Получает реферальную ссылку через бота
   - Переходит по ссылке и регистрируется в казино
   - Делает депозит в казино
   - Отправляет подтверждение администратору
   - Не может получать сигналы до подтверждения

2. **Пользователь с депозитом:**
   - Полный доступ ко всем функциям
   - Может получать сигналы
   - Видит информацию о своем депозите

3. **Процесс подтверждения депозита:**
   - Пользователь нажимает "🎰 Получить реферальную ссылку"
   - Получает уникальную ссылку с его ID
   - Регистрируется в казино по этой ссылке
   - Делает депозит в казино
   - Нажимает "✅ Я сделал депозит" и отправляет подтверждение
   - Администратор получает уведомление
   - Администратор подтверждает через `/approve_deposit <user_id> [сумма]`
   - Пользователь получает уведомление и доступ к сигналам

## Решение проблем

**Бот не запускается:**
- Проверьте, что токен правильный
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`

**Ошибка "У вас нет прав":**
- Проверьте, что ваш ID добавлен в `ADMIN_IDS`
- Убедитесь, что используете правильный Telegram ID

**База данных не создается:**
- Проверьте права на запись в папке
- Убедитесь, что Python имеет доступ к файловой системе

## Поддержка

- Менеджер: [@nomep999](https://t.me/nomep999)
- ТГК: [@maksoncikaz](https://t.me/maksoncikaz)
